{
  "name": "@tailor.me/chrome-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Chrome extension for extracting LinkedIn job data and sending it to tailor.me",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.322",
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0"
  }
}
