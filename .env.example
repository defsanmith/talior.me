# ─────────────────────────────────────────────────────────────────────────────
# Defaults below are set for Docker Compose (uses container service names).
# For local development (pnpm dev), swap service names with localhost:
#   DATABASE_URL  → replace @postgres: with @localhost:
#   REDIS_HOST    → replace redis with localhost
#   OPENSEARCH_URL → replace http://opensearch: with http://localhost:
#   LATEX_SERVICE_URL → replace http://latex: with http://localhost:
# ─────────────────────────────────────────────────────────────────────────────

# Database
DATABASE_URL="postgresql://postgres:postgres@postgres:5432/tailorme?schema=public"

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# AI Provider Configuration
# Choose between "OPENAI" or "GEMINI" (default: OPENAI)
AI_PROVIDER="OPENAI"

# OpenAI Configuration
OPENAI_API_KEY="sk-..."
OPENAI_PARSE_MODEL="gpt-4o-mini"
OPENAI_REWRITE_MODEL="gpt-4o-mini"
OPENAI_SELECTION_MODEL="gpt-4o-mini"

# Google Gemini Configuration
GEMINI_API_KEY="..."
GEMINI_PARSE_MODEL="gemini-2.0-flash-exp"
GEMINI_REWRITE_MODEL="gemini-2.0-flash-exp"
GEMINI_SELECTION_MODEL="gemini-2.0-flash-exp"

# API
API_PORT=3001

# Worker
WORKER_CONCURRENCY=10

# OpenSearch (BM25 resume flow)
OPENSEARCH_URL=http://opensearch:9200
OPENSEARCH_USERNAME=admin
OPENSEARCH_PASSWORD=admin
OPENSEARCH_INDEX_BULLETS=bullets

# Auth
# Change this to a long random string in production!
JWT_SECRET=change-me-in-production

# Inter-service URLs
# LATEX_SERVICE_URL / OPENSEARCH_URL use Docker service names (see note above).
# NEXT_PUBLIC_BASE_URL and CORS_ORIGIN are browser-facing — keep as localhost.
LATEX_SERVICE_URL=http://latex:3002
NEXT_PUBLIC_BASE_URL=http://localhost:3001
CORS_ORIGIN=http://localhost:3000
